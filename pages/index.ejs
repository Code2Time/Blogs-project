<html lang="en">
  <%- include('../partials/head.ejs') %>
  <body>
    <%- include('../partials/nav.ejs') %> <% if(blogsData.length > 0){ %> <%
    blogsData.forEach(blog => {%>
    <h1><%=blog.title%></h1>
    <p><%=blog.desc%></p>
    <%});%> <%}else{%>
    <p>ther is not blog</p>
    <% }%> <%- include('../partials/footer.ejs') %>
  </body>
</html>
